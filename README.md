# Chrono

Chrono is a lightweight daily schedule manager built with React, TypeScript, Tailwind CSS, and Supabase.

## Development

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

Build and lint:

```bash
npm run build
npm run lint
```
